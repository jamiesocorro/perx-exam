import { UpdateTextDirective } from './update-text.directive';

describe('UpdateTextDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateTextDirective();
    expect(directive).toBeTruthy();
  });
});
